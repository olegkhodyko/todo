import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  form: {
    paddingVertical: 10,
    borderTopWidth: 1,
    borderColor: '#d1d5db',
    gap: 16,
  },
  descriptionInput: {
    height: 60,
  },
});
